# Candidatos a golden (auto-melhoria)

Arquivos gerados por `npm run eval:harvest` a partir de interações reais que
merecem virar caso de teste (bloqueadas, com erro, abstidas ou de baixa qualidade).

Fluxo: revise cada candidato, ajuste a rubrica/asserções e mova os bons para
`evals/cases/`. Este diretório N�O é avaliado pela bateria.
